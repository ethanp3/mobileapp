<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Notes.Views.NoteEntryPage"
             Title="Add to Collection">
    <ContentPage.Resources>
        <!-- Implicit styles -->
        <Style TargetType="{x:Type Editor}">
            <Setter Property="BackgroundColor"
                    Value="{StaticResource AppBackgroundColor}" />
        </Style>
    </ContentPage.Resources>

    <!-- Layout children vertically -->
    <StackLayout Margin="{StaticResource PageMargin}">
        <Editor Placeholder="Description"
                Text="{Binding Text}"
                HeightRequest="100" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Layout children in two columns -->

            <Image x:Name="imgCam" Grid.Row="0" Grid.Column="0"/>

            <Button x:Name="btnCam" Text="Camera" Grid.Row="0" Grid.Column="0" Margin="2"
                    Clicked="BtnCam_Clicked"
                    BackgroundColor="#00a4fc" TextColor="#FFFFFF" />
            <Button x:Name="imgSelect" Text="Pick Image" Grid.Row="0" Grid.Column="1" Margin="2"
                    Clicked="OnPickImagesClick" />

            <Button Text="Save" Grid.Row="2" Grid.Column="0" Margin="2"
                    Clicked="OnSaveButtonClicked" />
            <Button Text="Delete" Grid.Row="2" Grid.Column="1" Margin="2"
                    Clicked="OnDeleteButtonClicked" />
        </Grid>
    </StackLayout>
</ContentPage>