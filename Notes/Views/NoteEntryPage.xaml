<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Notes.Views.NoteEntryPage"
             Title="Add to Collection">
    <ContentPage.Resources>
        <!-- Implicit styles -->
        <Style TargetType="{x:Type Editor}">
            <Setter Property="BackgroundColor"
                    Value="{StaticResource AppBackgroundColor}" />
        </Style>
    </ContentPage.Resources>

    <!-- Layout children vertically -->
    <StackLayout Margin="{StaticResource PageMargin}">
        <Editor Placeholder="Description"
                Text="{Binding Text}"
                HeightRequest="100" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Layout children in two columns -->

            <Button x:Name="btnCam" Text="Camera" Grid.Row="1" Grid.Column="0" 
                    Clicked="BtnCam_Clicked"
                    BackgroundColor="#00a4fc" TextColor="#FFFFFF" />
            <Button x:Name="imgSelect" Text="Pick Image" Grid.Row="1" Grid.Column="1" 
                    Clicked="OnPickImagesClick" />

            <Button Text="Save" Grid.Row="2" Grid.Column="0" 
                    Clicked="OnSaveButtonClicked" />
            <Button Text="Delete" Grid.Row="2" Grid.Column="1" 
                    Clicked="OnDeleteButtonClicked" />

            <Image x:Name="imgPre" Grid.Row="0" Grid.Column="0" />
            <Label x:Name="imgLbl" Grid.Row="0" Grid.Column="1" />
            
        </Grid>
    </StackLayout>
</ContentPage>